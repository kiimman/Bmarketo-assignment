@model UptosellCollectionViewModel


<section class="up-to-sell">
    <div class="container">
        <div class="collection-grid">
            <!-- collection-item start -->
            <a asp-controller="products" asp-action="details" asp-route-id="@Model.Id" class="collection-item">
                <img src="@Model.ImageUrl" alt="@Model.Title">
                <div class="body">
                    <div class="title">@Model.Title</div>
                    <div class="info">
                        <div class="price">@Model.Price</div>
                        <i class="add-cart fa-regular fa-cart-plus"></i>
                    </div>
                </div>
            </a>
            <!-- collection-item end -->
            <!-- collection-item start -->
            <div class="collection-item collection-content">
                <div class="container">
                    <div class="title-red">@Model.TitleRed</div>
                    <div class="title">@Model.UptoSellTitle</div>
                    <div class="ingress">@Model.Ingress</div>
                    <div class="text">@Model.Text</div>
                    <a href="#" class="link">Discover More</a>
                </div>
            </div>
            <!-- collection-item end -->
             <!-- collection-item start -->
            <a asp-controller="products" asp-action="details" asp-route-id="@Model.Id" class="collection-item">
                <img src="@Model.ImageUrl" alt="@Model.Title">
                <div class="body">
                    <div class="title">@Model.Title</div>
                    <div class="info">
                        <div class="price">@Model.Price</div>
                        <i class="add-cart fa-regular fa-cart-plus"></i>
                    </div>
                </div>
            </a>
            <!-- collection-item end -->
            
        </div>
    </div>
</section>    